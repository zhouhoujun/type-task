import { PipeSourceActivity } from '../core';
import { AssetTask } from '../decorators';

/**
 * pipe component
 *
 * @export
 * @class PipeComponent
 * @extends {TaskElement}
 * @implements {IPipeComponent<ITransform>}
 */
@AssetTask
export class PipeAsset extends PipeSourceActivity {

}
